#This is a shell script to execute commands which we previously executed via terminal directly
pwd 
ls -l
banner "Script ended"

#The commands will be executed in the sequence in which the commands are written in this script